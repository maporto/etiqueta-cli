/* jshint -W117, -W030 */
describe('etiqueta', function() {
  describe('state', function() {
    var views = {
      etiqueta: 'app/etiqueta/etiqueta.html'
    };

    beforeEach(function() {
    });

    it('should map /etiqueta route to 404 View template', function() {
      expect(true).to.equal(true);
    });
  });
});
